kind: PolicySequence
apiVersion: api.webmethods.io/beta
metadata:
  name: policySequence
  namespace: dev
  version: '1.0'
  tags: []
spec:
  transport:
    - $ref: dev:InbMessaging:1.0
    - $ref: dev:transport-test:1.0
    - $ref: dev:transport:1.0
  iam:
    - $ref: dev:IdentifyAndAuthorize:1.0
  routing:
    - $ref: dev:StraightThroughRoutingPolicy:1.0
  monitoring:
    - $ref: dev:LogInvocation:1.0
