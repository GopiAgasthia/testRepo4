kind: PolicySequence
apiVersion: api.webmethods.io/beta
metadata:
  name: policy-sequence
  namespace: dev
  version: '1.0'
  tags: []
spec:
  transport:
    - $ref: dev:transport:alpha
    - $ref: dev_test:SetMediaType:1.0
  routing:
    - $ref: dev:loadbalance:1.0
    - $ref: dev:straightthrough:1.0
  iam:
    - $ref: dev:IdentifyAndAuthorize:1.0
